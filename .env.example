# ============================================================================
# Tesla Stock Analysis Multi-Agent System - Environment Configuration
# ============================================================================

# REQUIRED: Google Gemini API Key
# Get your free API key from: https://makersuite.google.com/app/apikey
GOOGLE_API_KEY=your_api_key_here

# ============================================================================
# OPTIONAL CONFIGURATION (Defaults shown below)
# ============================================================================

# Stock Configuration
# -------------------
# Target stock symbol to analyze
STOCK_SYMBOL=TSLA

# Number of years of historical data to analyze during initialization
HISTORICAL_YEARS=5

# Analysis Thresholds
# -------------------
# Minimum percentage price change to identify as significant event (%)
SIGNIFICANT_CHANGE_THRESHOLD=5.0

# Minimum similarity score for matching current conditions to historical events (0.0-1.0)
# Higher values = more strict matching, fewer but better matches
SIMILARITY_THRESHOLD=0.85

# Time window (hours) to consider for event context analysis
CONTEXT_WINDOW_HOURS=72

# Database Configuration
# ----------------------
# Directory path for Chroma vector database persistence
CHROMA_PERSIST_DIR=./chroma_db

# Collection name for storing event embeddings
COLLECTION_NAME=tesla_events

# Embedding Model
# ---------------
# Sentence transformer model for generating embeddings
# Options: all-MiniLM-L6-v2 (fast), all-mpnet-base-v2 (better quality)
EMBEDDING_MODEL=all-MiniLM-L6-v2

# Logging
# -------
# Logging level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO
